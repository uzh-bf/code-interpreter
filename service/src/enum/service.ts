export enum Jobs {
  execute = 'execute',
}

export enum Queues {
  python = 'python-queue',
  other = 'other-queue',
}

export enum Languages {
  bash = 'bash',
  js = 'js',
  node = 'node',
  py = 'py',
  ts = 'ts',
}
